<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sale.action_quotations_with_onboarding"
            model="ir.actions.act_window">
        <field name="domain">[('is_revision', '=', False)]</field>
    </record>

    <record id="sale.action_orders"
            model="ir.actions.act_window">
        <field name="domain">[('is_revision', '=', False)]</field>
    </record>

</odoo>